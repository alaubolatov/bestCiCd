name: "Check Deployment Action"
description: "Checks the deployment status"

inputs:
  environment:
    description: "Name of the environment"
    required: true

runs:
  using: "composite"
  steps:
    - name: Say Hello
      shell: bash
      run: echo "Hello from check deployment action in the ${{ inputs.environment }} environment!"